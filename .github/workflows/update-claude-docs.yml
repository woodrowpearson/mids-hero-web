name: Update Claude Documentation

# Workflow disabled - automated timestamp updates provide minimal value
# Update documentation timestamps manually during actual doc changes
on:
  workflow_dispatch:
    inputs:
      force_update:
        description: 'Force documentation update (workflow disabled)'
        required: false
        default: 'false'

jobs:
  update-docs:
    runs-on: ubuntu-latest

    steps:
      - name: Workflow disabled
        run: |
          echo "❌ This workflow is disabled"
          echo ""
          echo "Reason: Automated timestamp updates create CI noise for minimal value"
          echo ""
          echo "Recommendation: Update documentation timestamps manually during"
          echo "actual documentation changes, not automatically on every code push."
          echo ""
          echo "If you need to update timestamps, use:"
          echo "  just update-docs"
          exit 0